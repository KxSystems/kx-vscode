---
type: Reference
title: "PR #796 Review — Resolutions"
description:
  "Every review comment on the KX Query View pull request, with what ee-webviews
  changed in answer or why it changed nothing."
timestamp: 2026-09-11
tags: [github, review, query-editor, vscode-extension]
---

# PR #796 Review — Resolutions

- **Pull request**:
  [#796 KX Query View](https://github.com/KxSystems/kx-vscode/pull/796)
  (`ee-webviews` into `dev`)
- **Review**: @cdinsmore-kx, 10–11 September 2026 — nine inline comments, three
  medium and three low findings, and one report in a review body
- **Related**: [Query Editor](../user/query-editor.md) (user documentation),
  [KXI Datasource Issues — `ee-webviews` Worklist](kxi-datasource-issues.md)

Fourteen of the sixteen are fixed on the branch, one of them in part, and two
change nothing, for the reasons given. Findings 10 to 14 are about code that is
already in `dev` rather than code this pull request adds; each row says so, and
all but the REPL one (13) are fixed here nonetheless. No reply has been posted
to the pull request yet.

| #   | Comment                                                                                                                                                                                                                                                                                                                                 | Verdict                      | Resolution                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | [`src/models/uda.ts:23`](https://github.com/KxSystems/kx-vscode/pull/796#discussion_r3978202834) — the `scope` help says "an assembly on its own leaves the instance to the resource coordinator", but no target can be picked without a tier, a DAP or `distributed` after it.                                                         | Fixed                        | Right: the dropdown shows the bare assembly as `<assembly> distributed`, so the help now uses that word. `scope` reads "Tier or DAP process to run the request on, or distributed to leave the choice of DAPs to the resource coordinator", and the qSQL `target` help and the Scope section of the user documentation say the same.                                                                                                                                                                                                                                                                                                |
| 2   | [`src/webview/components/kdbQueryView.ts:669`](https://github.com/KxSystems/kx-vscode/pull/796#discussion_r3978784434) — could a label value outside the suggestions be needed? Typing one hides the list, which only appears for an empty or exactly matching input; it should behave like the table select.                           | Fixed                        | Yes: the suggestions come from the connection's meta, and a file written against another connection has to keep the labels it names, so free text stays. The label key and value fields are now `kdb-select` in a new `editable` mode — the searchable dropdown the table field uses, except that what is typed is the value. Typing narrows the list rather than hiding it, and Enter keeps the typed text unless a suggestion was walked to with the arrow keys.                                                                                                                                                                  |
| 3   | [`src/webview/components/kdbQueryView.ts:669`](https://github.com/KxSystems/kx-vscode/pull/796#discussion_r3978859741) — once a key is picked, the suggestions do not come back to pick a different one.                                                                                                                                | Fixed                        | Same change. The native `<datalist>` filtered by the text already in the field; `kdb-select` opens on every suggestion whatever the field holds. A UDA's `labels` value, which takes several values, holds each as a badge (`editable multiple`): Enter, Space or `;` adds what is typed, and a suggestion adds itself.                                                                                                                                                                                                                                                                                                             |
| 4   | [`src/models/query.ts:189`](https://github.com/KxSystems/kx-vscode/pull/796#discussion_r3978996283) — the calendar icon on `startTS` and `endTS` is black and barely visible in a dark theme.                                                                                                                                           | Fixed                        | Webviews drew native controls in the light colour scheme whatever the theme. The shared page reset, `webviewReset`, now sets `color-scheme: dark` when VS Code marks the page `vscode-dark` or `vscode-high-contrast` (`data-vscode-theme-kind`), so the date picker icon — and every other native control in every webview — follows the theme.                                                                                                                                                                                                                                                                                    |
| 5   | [`src/webview/components/kdbQueryView.ts:317`](https://github.com/KxSystems/kx-vscode/pull/796#discussion_r3979028239) — "Select a table first..." could say "Select a table to view available columns", consistent with the labels hint "Name a table to see the labels it carries."                                                   | Fixed                        | A column dropdown with no table to draw on says `Select a table to view available columns...`, and the labels hint reads `Select a table to view only the labels it carries.` — "only", since labels are suggested before a table is chosen too, just not narrowed to one.                                                                                                                                                                                                                                                                                                                                                          |
| 6   | [`src/utils/query.ts:189`](https://github.com/KxSystems/kx-vscode/pull/796#discussion_r3979251419) — a `limit` of -3 returns an empty table, while the same parameters return data in the Insights web interface.                                                                                                                       | Not changed — not reproduced | The editor sends the number as typed (`Number("-3")`, with no handling of the sign). Against a live kdb Insights Enterprise 1.20 instance, getData with `limit: -3` returned the last 3 of 100 rows by every route the extension takes: the gateway as JSON, the gateway as struct-text with the `opts.timeout` the extension adds, and the scratchpad import behind **Populate Scratchpad**. Something else in the request must differ; the `.kxquery` file and the instance it ran against are needed to go further.                                                                                                              |
| 7   | [`src/webview/components/kdbQueryView.ts:246`](https://github.com/KxSystems/kx-vscode/pull/796#discussion_r3979307406) — `startTS` and `endTS` added to `.kxi.preview` show dates far in the past and future; they should start the way getData's and a UDA's do.                                                                       | Fixed                        | Those dates are the defaults the preview API registers — the unbounded timestamps `1707-09-22T00:12:43.145224193` and `2292-04-10T23:47:16.854775807` — and the form showed a default as if it had been entered. `parseUDAParams` now drops a timestamp default at either end, so the fields start blank like getData's; a blank field is left out of the request, which asks for exactly that range. `limit` keeps its default of 1000.                                                                                                                                                                                            |
| 8   | [`src/webview/components/kdbQueryView.ts:319`](https://github.com/KxSystems/kx-vscode/pull/796#discussion_r3979359812) — the "a" or "an" is not always wanted: `columns` reads "Select a columns...".                                                                                                                                   | Fixed                        | Parameter names come from UDA metadata (`sortCols`, `byCols`, `API`), so no rule picks the right article for every one. Placeholders drop it: `Select columns...`, `Select table...`, `Select API...`.                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 9   | [`src/webview/components/kdbSelect.ts:309`](https://github.com/KxSystems/kx-vscode/pull/796#discussion_r3988435129) — should a required select offer an empty "Select a table" or "Select a target" entry? It could work like **+ Add parameter**.                                                                                      | Fixed                        | No. A parameter that has to be given — getData's `table`, qSQL's `target`, any required UDA choice — and the operator of a filter or aggregation row now render `kdb-select required`, the mode **+ Add parameter** uses, which has no empty entry. Optional choices such as `fill` keep it, since choosing it is how they are cleared. A required list with nothing in it says `No options`.                                                                                                                                                                                                                                       |
| 10  | [Medium 1](https://github.com/KxSystems/kx-vscode/pull/796#pullrequestreview-5176918128) — `src/utils/plotUtils.ts:135`: `writeToCell` uses `cell.index`, which is -1 once an earlier image replaced the cell, so a second late image throws and lands in a `.plot` file.                                                               | Fixed                        | **The code is in `dev`; fixed here nonetheless.** Confirmed. `writeToCell` now takes the target and, once the edit applies, moves it on to the cell written in its place, so every later image goes to the live cell. Test: two images arriving after the cell ended both land in it.                                                                                                                                                                                                                                                                                                                                               |
| 11  | [Medium 2](https://github.com/KxSystems/kx-vscode/pull/796#pullrequestreview-5176918128) — `src/classes/scratchpadLogger.ts:69`: a disconnect while `connect()` awaits the token is lost, and the socket opened afterwards can never be closed.                                                                                         | Fixed                        | **The code is in `dev`; fixed here nonetheless.** Confirmed: `open()` reset `isManualClose` after the await, undoing the disconnect. `connect()` now clears the flag before awaiting, and `open()` stops when a disconnect arrived in the meantime. Test: disconnecting while the token is pending opens no socket.                                                                                                                                                                                                                                                                                                                 |
| 12  | [Medium 3](https://github.com/KxSystems/kx-vscode/pull/796#pullrequestreview-5176918128) — `src/commands/serverCommand.ts:1385`: an Insights image result writes the `.plot` and returns before the destination check, so nothing reaches the console and the query never enters history; the local path nests the check the other way. | Fixed in part                | **The code is in `dev`; fixed here nonetheless.** History: accepted. A PNG result was never recorded, on the local path either, and both now go through `writeQueryResultsToPlot`, which draws the chart and records the query. Order: kept. An image has no readable form in a console, so drawing it whatever the destination is intended, and the chart opening beside the file is where the result lands. The local path cannot follow: with the console destination q stringifies the result before it returns, which leaves no image bytes to detect.                                                                         |
| 13  | [Low 1](https://github.com/KxSystems/kx-vscode/pull/796#pullrequestreview-5176951060) — `src/classes/replConnection.ts:535`: `executeQuery` lost its token, so a notebook cell's stop button interrupts whatever the shared REPL is running and resolves every queued execution as cancelled.                                           | Not changed                  | **The code is in `dev`; not fixed here.** Accurate, and `replConnection.ts` is identical on `dev`. Stopping the running statement and dropping the queue behind it is the REPL's interrupt — the one Ctrl+C gives in its terminal — so nothing queued runs against state the user has just interrupted. What is wrong is narrower: a cell still waiting in the queue interrupts someone else's statement rather than only leaving the queue. That belongs with the REPL work in progress.                                                                                                                                           |
| 14  | [Low 2](https://github.com/KxSystems/kx-vscode/pull/796#pullrequestreview-5176951060) — `src/services/connectionManagerService.ts:329`: when the tree node is missing, `disconnect` returns before `disconnectBehaviour`, leaving a disposed console registered for the label.                                                          | Fixed                        | **The code is in `dev`; fixed here nonetheless.** Confirmed, at `disconnect` (`:231`): the node was looked up and never used. `disconnect` now needs only the connected connection. Test: a connection whose tree node is gone is disconnected and cleaned up.                                                                                                                                                                                                                                                                                                                                                                      |
| 15  | [Low 3](https://github.com/KxSystems/kx-vscode/pull/796#pullrequestreview-5176951060) — `src/services/queryEditorProvider.ts:88`: the query cache is keyed by connection label and cleared only by **Refresh**, so reconnecting to a changed deployment serves the old APIs, tables and targets.                                        | Fixed                        | The parsed APIs, tables, targets and labels are held against the connection's meta object rather than its label. A reconnect and a meta refresh both replace that object, so each is read afresh, and **Refresh** has nothing left to clear by hand.                                                                                                                                                                                                                                                                                                                                                                                |
| 16  | [Review report](https://github.com/KxSystems/kx-vscode/pull/796#pullrequestreview-5177051114) — a query opened while disconnected still lists UDAs, and after switching connection the dropdowns offer UDAs and tables that connection does not have (`insightsMultitypeTable` beside `nsansUdaTable`).                                 | Fixed                        | Two causes. The cache in 15 was consulted before the connection was checked, so a disconnected label kept serving its last meta; the check now comes first, and only a connected meta is read. And `kdb-select` added the value a file holds to its options when the connection did not list it — `insightsMultitypeTable` was the file's own table, offered as though the connection had it. The value still shows in its field, so opening a file against another connection loses nothing, but the dropdown offers only what the connection reports, and the API picker still says when the chosen API is not on the connection. |
