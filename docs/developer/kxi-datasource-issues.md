---
type: Reference
title: "KXI datasource issues — ee-webviews worklist"
description:
  "KXI datasource issues assigned to the extension maintainer, every child of
  epic KXI-73189, and related issues added by hand, each open one classified
  against the code on the ee-webviews branch."
timestamp: 2026-09-03
tags: [jira, kxi, datasources, vscode-extension]
---

# KXI datasource issues — `ee-webviews` worklist

- **Related**:
  [RFC: Removing Datasources, Adding the Query Webview](rfc-kxquery.md) (why the
  editor was replaced), [Query Editor](../user/query-editor.md) (user
  documentation)

## Fixed (21)

| Key                                                     | Type     | Priority      | Summary                                                                                                               | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Epic                                                    |
| ------------------------------------------------------- | -------- | ------------- | --------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------- |
| [KXI-69949](https://kxl.atlassian.net/browse/KXI-69949) | Story    | Medium        | Add Preview tab to the Data Source page                                                                               | `parsePreviewApi` offers `.kxi.preview` wherever the meta lists it — the params are the ones the deployment registers, so there is no signature kept here and no version gate — and it runs down the UDA path to `servicegateway/kxi/preview`. `isUDAAvailable` had to stop reading the `uda` flag as proof, and a blank optional parameter is dropped rather than sent for the gateway to cast. Verified against amber 2026-09-03: all four gateway spellings answer 200, and the real meta drives the form to `{"table":"udaTable"}` / `{"table":"udaTable","limit":5}` — the bodies that returned 100 and 5 rows.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | —                                                       |
| [KXI-70349](https://kxl.atlassian.net/browse/KXI-70349) | Bug      | High          | v1 GetMeta being used by the VSCode datasource instead of the latest version                                          | Only `api/v3/meta` remains; landed in v1.13.0 (da784bec, e1ca8f17) — seven releases back, not by this work, so a getMeta error after 1.13 needs a fresh diagnosis.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | [KXI-73189](https://kxl.atlassian.net/browse/KXI-73189) |
| [KXI-70493](https://kxl.atlassian.net/browse/KXI-70493) | Story    | High          | Datasource - UDA table parameter should be updated to a search-select                                                 | `source: "tables"` routes the UDA table param to the filterable `kdb-select`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | [KXI-73189](https://kxl.atlassian.net/browse/KXI-73189) |
| [KXI-70501](https://kxl.atlassian.net/browse/KXI-70501) | Task     | High          | Datasource timeout field should be moved to the status bar for consistency with workbooks and notebooks               | `isQuery(uri)` branch in `setTimeoutItem` shows the status-bar timeout item.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | [KXI-73189](https://kxl.atlassian.net/browse/KXI-73189) |
| [KXI-68887](https://kxl.atlassian.net/browse/KXI-68887) | Task     | Medium        | Unify datasource connection and timeout selection UI                                                                  | Query files use the same two status-bar items as workbooks and notebooks.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | [KXI-73189](https://kxl.atlassian.net/browse/KXI-73189) |
| [KXI-70471](https://kxl.atlassian.net/browse/KXI-70471) | Bug      | Medium        | Adding a new datasource doesn't generate a unique name which risks dataloss if the existing datasource is overwritten | `addWorkspaceFile` increments `query-N.kxquery`, checking disk and untitled docs; single extension now.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | [KXI-73189](https://kxl.atlassian.net/browse/KXI-73189) |
| [KXI-70490](https://kxl.atlassian.net/browse/KXI-70490) | Bug      | Medium        | Datasource - qSQL target dropdown is difficult to read if there are multiple targets and instances                    | `source: "targets"` gives the target dropdown search; not split into two dropdowns.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | [KXI-73189](https://kxl.atlassian.net/browse/KXI-73189) |
| [KXI-69253](https://kxl.atlassian.net/browse/KXI-69253) | Bug      | To be defined | Datasource requests aren't setting timeouts                                                                           | Provider passes `getTimeoutForUri().value` on Run and Populate; forwarded to all four query types.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | [KXI-73189](https://kxl.atlassian.net/browse/KXI-73189) |
| [KXI-70489](https://kxl.atlassian.net/browse/KXI-70489) | Bug      | To be defined | Datasource - start time and end time datepicker issues                                                                | Native `datetime-local` + 9-digit nanos field; the custom picker that would not close was deleted.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | [KXI-73189](https://kxl.atlassian.net/browse/KXI-73189) |
| [KXI-69680](https://kxl.atlassian.net/browse/KXI-69680) | Story    | Highest       | VSCode: Support the new qSQL distributed option                                                                       | `parseTargets` offers the assembly on its own from Insights 1.13, `generateQSqlBody` leaves `tier` and `dap` out of its scope, and the target quick-pick offers it as well.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | —                                                       |
| [KXI-65951](https://kxl.atlassian.net/browse/KXI-65951) | Task     | To be defined | VSCode to allow for specifying UDA parameter type when there are multiple registered                                  | Fixed 2026-08-31. The `renderMultitype` dropdown was cosmetic: `resolveParamType` returned `param.type[0]` regardless of the pick, so the `parameterTypes` key already sent to the scratchpad (insightsConnection.ts:642) always carried the first registered type. It now resolves `selectedMultiTypeString`. Run Query drops `parameterTypes` and that is correct, not pending — a REST request has nowhere to put them and the gateway casts from the first type in the list ([`.kxi.metaParam`](https://code.kx.com/insights/1.16/api/database/uda/uda-creating.html)) — so the caveat is stated at the dropdown and repeated by `recastParams` before sending, both quiet unless the pick differs from the first type. `.e2eUda.identityAPI` in the e2e fixture is the ticket's own example UDA.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | [KXI-73189](https://kxl.atlassian.net/browse/KXI-73189) |
| [KXI-70491](https://kxl.atlassian.net/browse/KXI-70491) | Bug      | Medium        | Datasource - API does not have a Select Columns filter                                                                | Fixed 2026-08-31. A `columns` parameter using the multi-select `kdb-select` already in the codebase. getData has no column-projection parameter — `agg` doubles as one, taking "column(s) to select" as a symbol list — so `buildGetDataPayload` sends `columns` under the `agg` key, and a request filling both is refused with a message. Context for any "my getData filter is ignored" report: the API transport used to hardcode `body.params` to `table`/`startTS`/`endTS`, discarding filter, groupBy, agg, sortCols, limit, labels and scope. This branch sends the whole payload.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | [KXI-73189](https://kxl.atlassian.net/browse/KXI-73189) |
| [KXI-71824](https://kxl.atlassian.net/browse/KXI-71824) | Story    | High          | [vscode] Display stack traces for UDA and qsql queries                                                                | Fixed 2026-09-01. Run Query kept only the message: both failure branches of `runDataSource` collapsed the response to `res.errorMsg ? res.errorMsg : res.error`, dropping the trace. `getDatasourceQuery` now carries `header.bt` — the gateway's own error header, the field the scratchpad service reads as `bt` beside `ai` — out as `stacktrace`, `parseError` keeps it beside the error and `formatDataSourceError` appends it for both the results view and the console. `appendStacktrace` is the one place that formats a trace, string or frame list, shared with `writeScratchpadResult`, which held the only copy. Display needed two fixes for the caret to mean anything: `.results-txt` had no CSS rule at all, so the results view rendered any text result proportional with collapsed spaces — it is now monospace `pre-wrap`, measured in the e2e suite by comparing the rendered width of `"  [0] {1+"` with `"        ^"`. And `formatResult` escaped nothing while the view injects with `unsafeHTML`, so `{x<y}` was parsed as a tag and swallowed the rest of the trace; text results are escaped now, and the e2e test that pinned the markup rendering asserts text instead. Populate Scratchpad is the exception: `notify` runs its message through `stripUnprintableChars`, which eats newlines, so the trace rides in the log params and reaches the output channel under **Details** rather than a mangled toast — surfacing it properly there belongs to KXI-73220, which is about that path showing errors at all. Not reproducible against a server yet: the gateway sends `bt` only where the error is in user code and the caller may see traces. | —                                                       |
| [KXI-70494](https://kxl.atlassian.net/browse/KXI-70494) | Story    | High          | Datasource - UDA labels parameter should be updated to a key-value pair field                                         | Fixed 2026-08-31 — but see the unfiled row on dictionary parameters: the grid is right and its value is still rejected by the gateway as a string. The UDA `labels` parameter is a repeatable key/value grid; the `isDictionary` branches of `parseRows`/`serializeRows` now honour `UDAParamField.many`, which they previously ignored. One key takes several space- or semicolon-separated values, a single value is still emitted as a list (`{"region":["canada"]}`), and values stay strings so `600519` is not coerced to a number. getData's own `labels` filter stays single-valued — the one intended difference.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | [KXI-73189](https://kxl.atlassian.net/browse/KXI-73189) |
| [KXI-69283](https://kxl.atlassian.net/browse/KXI-69283) | Bug      | Medium        | Vscode suppresses query errors on RC or SG                                                                            | Fixed 2026-09-01. Root cause was one unguarded handler: `getDatasourceQuery`'s axios `.catch` read `error.response.data.header`, which only a gateway error carries. A killed coordinator answers with a plain-text 500, a killed gateway with openresty's 502 HTML, and a dropped socket with no response at all — on each, the handler itself threw a `TypeError`, which `runDataSource` logged at `DEBUG` and never showed, taking the query history entry with it. The July fix (31d7a66d) added `extractInsightsRequestError` but wired it only into `getScratchpadQuery`/`importScratchpad`, which is exactly why Ben's 2026-08-26 comment saw Populate Scratchpad report the error and the Run button stay silent. `getDatasourceQuery` now uses the same helper and optional-chains the header; `runDataSource`'s catch raises to `ERROR`, records a failed history entry and resets `isDatasourceExecution`, rethrowing for `.kxnb` so a notebook fails the cell that raised it instead of reporting success; the query editor reports a rejected `Runner` instead of dropping it, and Populate Scratchpad gained the progress notification it was missing (Ben's third bullet). Also fixed the same unguarded read in `cancelScratchpad`. Proven by two e2e tests that kill the RC and the SG through the stand-in and fail without the fix.                                                                                                                                                                                                                                                                                                                              | [KXI-69362](https://kxl.atlassian.net/browse/KXI-69362) |
| [KXI-73220](https://kxl.atlassian.net/browse/KXI-73220) | Bug      | To be defined | Show error messages returned by qsql queries                                                                          | Fixed — but not by this work, and before the ticket was filed. The reported log line (`Error occured while populating scratchpad. {"status":200}`) lost the message because `importScratchpad` did not interpolate `response.data.errorMsg`; df9fbefc (2026-07-20) added it, and the ticket was raised 2026-08-24 against an older build. Same family as KXI-69283 but a different site: this is an HTTP 200 carrying `{error:true,errorMsg}`, not the unguarded `error.response.data.header` read. Verify against a current build before closing.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | [KXI-73189](https://kxl.atlassian.net/browse/KXI-73189) |
| [KXI-70614](https://kxl.atlassian.net/browse/KXI-70614) | Bug      | High          | Stack traces aren't shown correctly in the KDB RESULTS tab                                                            | Added by hand 2026-09-01 — a child of the results epic, not of KXI-73189, so the original pull missed it. Fixed by 6dcfa5c0 on three fronts: the gateway stack trace was dropped entirely (`insightsConnection.ts` now carries `header.bt` through as `stacktrace` and logs it), `formatResult` interpolated the trace straight into HTML so `<` and `>` in it were eaten by the parser (`escapeHtml` now escapes them), and `.results-txt` had no `white-space` rule so the trace collapsed onto one line (now `pre-wrap` in the editor font). **Caveat**: the ticket body is empty, so the match to what Ben actually saw is inferred from the symptoms — confirm with him before closing.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | [KXI-73198](https://kxl.atlassian.net/browse/KXI-73198) |
| [KXI-34721](https://kxl.atlassian.net/browse/KXI-34721) | Bug      | High          | VSCode: Certain q queries do not work with datasources qsql                                                           | Tested live 2026-09-01 — does not reproduce; fixed, but not by this work. PINK has `.kxi.qsql` disabled at the gateway (`.kxi.qsql not enabled, add to 'KXI_ALLOWED_SBX_APIS' envvar or enable 'queryEnvironments'`, on all three endpoint spellings), so the run was on amber `1.20.0-rc.157`: `count udaTable` answers **200** with `{count: 1, columns: [{name: "values", type: "long", values: ["100"]}]}`, and the real `convertToGrid`/`updatedExtractRowData` turn that into a one-row grid and a printed console table — against 100 rows for `select from udaTable`. Atoms, vectors, dictionaries, keyed tables and general lists all render too (`1+1`, `til 5`, `` `a`b!1 2 ``, `1!select from udaTable`, `(1;2.5;`a)`, `meta udaTable`). What the reporter hit is now an error rather than silence: q the DAP rejects (`exec x1 from t`, `first`/`last t`on a partitioned hdb,`max x1 from t`, `` `sym$`a ``, `{x+1}`) comes back **HTTP 400** carrying `header.ai`and`header.bt`, which axios rejects into `getDatasourceQuery`'s catch and KXI-69283/KXI-71824 surface. Re-open only with a query that answers 200 and still renders nothing.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | [KXI-73189](https://kxl.atlassian.net/browse/KXI-73189) |
| [KXI-73119](https://kxl.atlassian.net/browse/KXI-73119) | Bug      | To be defined | `1b and `0b are being incorrectly changed to true and false                                                           | Fixed 2026-09-01. Not `kdbToAgGridCellType` as first triaged — the values never reach it: the results view prints `row[col.field]` and ignores `cellDataType` entirely. The coercion was in `decodeQUTF` (`src/utils/decode.ts`), which special-cased `"1b"`/`"0b"` into JS booleans on the way through `updatedExtractRowData`, so a struct-text payload that already said `1b` came out `true`. Removed; `booleans`/`boolean` now map to `text` in `kdbToAgGridCellType` too, so the declared type matches what is rendered. Verified against the live payload from PINK's `insightsMultitypeTable` (`cboolean` renders `1b`/`0b`), with a `convertToGrid` regression test and the inverted `decodeQUTF` unit test; the full suite passes. Also hardening: the same function feeds `writeQueryResultsToConsole` (`serverCommand.ts:1285`, which calls `res.startsWith`) and `scratchpadLogger`'s stdout/stderr appends, both of which a boolean return would break — no reachable input producing a bare `1b` there was found, so it is a removed hazard rather than a fixed crash. Note the filter side is a separate, still-open defect: `1b` typed into a boolean filter matches the `0b` rows (last row of the unfiled table).                                                                                                                                                                                                                                                                                                                                                                                                                                                | —                                                       |
| [KXI-70496](https://kxl.atlassian.net/browse/KXI-70496) | Story    | High          | Datasource - UDA scope parameter should be updated to populated widget                                                | Fixed 2026-09-03, and wider than filed: `scope` is the qSQL target dropdown everywhere it is asked — getData, UDA, and a UDA declaring its own — so the question is put once, in one widget, in one set of words (`SCOPE_DESCRIPTION`). Shape decided 2026-08-31: one targets search-select, following KXI-70490 rather than the ticket's three cascading fields, and no new fetch — `parseTargets` already builds the list from the meta. `sourceForParam` recognises the name ahead of the field type, which a dictionary can never satisfy, so a UDA declaring its own `scope` gets the widget too; `InsightsConnection.scopeForTarget` (extracted from `generateQSqlBody`, which now calls it) resolves the target against the meta, and `scopeValue` sends a dictionary through untouched so a `.kxquery` already holding scope as JSON text keeps working. The form and the file hold the target string, as qSQL's `selectedTarget` always has; the transport swaps it for the dictionary at the three send sites — `getApiBody`, `importScratchpad`'s API case and `retrieveUDAtoCreateReqBody`. Not yet exercised against a live gateway.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | [KXI-73189](https://kxl.atlassian.net/browse/KXI-73189) |
| [KXI-60352](https://kxl.atlassian.net/browse/KXI-60352) | Sub-task | Highest       | update the UDA fields at datasource of Vscode extension                                                               | Read the body 2026-08-31: this is the **designer's** ticket, not the extension's — "As UX designer I should create a new standard to be followed later in the vscode extension project" — and the parent story KXI-60351 is Deferred with the designer (Serkan Serbest). The only code-shaped requirement anyone stated is Philip Carneiro's comment on the parent, fields covering 70% of the form area, and the editor constrains the form instead (`.form { max-width: 60em }`, `queryStyles.ts`), which is the right shape for labelled fields. Closed 2026-09-03 on the second of the two ways out: the layout the new editor already has — one consistent field, label and help row per parameter — **is** the standard, so there is nothing left to adopt. Writing that standard down in the query-editor docs, and closing the parent against it, is still to do.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | [KXI-60351](https://kxl.atlassian.net/browse/KXI-60351) |

## Not fixed (2)

| Key                                                     | Type | Priority      | Summary                                           | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Epic                                                    |
| ------------------------------------------------------- | ---- | ------------- | ------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------- |
| [KXI-73276](https://kxl.atlassian.net/browse/KXI-73276) | Bug  | Medium        | Some datasource results are displayed incorrectly | Read the body and reproduced all six cases live 2026-09-01 (amber qsql). **The earlier "narrowed to booleans" note here was wrong** — it tested a flat typed table and missed every case the reporter lists. Four are fixable in the extension because the payload already carries what is needed: (a) **`isKey` is dropped** — a dictionary and a keyed table both arrive with `isKey: true` on their key columns and render as plain tables, which is the ticket's dict and keyed-table complaints; (b) **`attributes` is dropped** — `` `s#1 2 3 `` arrives as `attributes: "s"`, `` `g#`a`b`a `` as `"g"`, and neither reaches the header; (c) **empty results keep their schema in the payload** (`count: 0` with `isFile [booleans]`, `path [symbols]`; `()` is `general`, `()!()` is `key`+`values` with `isKey`) and the results view does render the header row — it is `ExecutionConsole.checkOutput` (`executionConsole.ts:93`) that replaces the whole thing with `No results found.` on an empty array, so the console is where the schema is lost; (d) a lambda's newlines **are** in the payload and the results view keeps them (`white-space: pre-line` collapses the indentation, `pre-wrap` would not) — the flattening in the ticket's paste is `convertRows` building a fixed-width console table. One case is not ours: an **enum** comes back as plain `longs` (`0 1 2`), the domain never leaves the DAP — needs an Insights-side fix, worth answering on the ticket. The `.Q.s`/`toString` proposal in the body would address all of it at once but replaces the grid with text; the four items above keep the grid. | [KXI-73189](https://kxl.atlassian.net/browse/KXI-73189) |
| [KXI-73189](https://kxl.atlassian.net/browse/KXI-73189) | Epic | To be defined | Datasource improvements                           | Epic — closes with its children. 18 children, all in this list: 13 fixed, 1 partially fixed, 1 not fixed, 1 rejected, 2 obsolete. KXI-65951 is missed by text search (it says "UDA parameter type", not "datasource"), so a re-pull needs both the assignee search and `parent = KXI-73189`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | —                                                       |

## Will not do — close (4)

| Key                                                     | Type  | Priority | Summary                                                                              | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                       | Epic                                                    |
| ------------------------------------------------------- | ----- | -------- | ------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------- |
| [KXI-65665](https://kxl.atlassian.net/browse/KXI-65665) | Story | Medium   | VSCode: Allow the user to choose the active connection for workbooks and datasources | Rejected 2026-08-31. Half the ask is already true — workbooks and notebooks follow the active connection. A query file cannot: it populates tables, targets and UDA parameters from one Insights instance's metadata, so `setRunScratchpadItemText` showing `(none)` rather than `(active)` for `.kxquery` is correct, not a gap. The test generalises: an active-connection default belongs to files whose content is connection-independent. | —                                                       |
| [KXI-70488](https://kxl.atlassian.net/browse/KXI-70488) | Bug   | Medium   | Datasource row limit allows negative values                                          | Rejected 2026-08-31 by decision. A signed `limit` is the API's own semantics — "a negative value takes them from the end" — and converted datasources arrive carrying `limit: -100000`, so a `min` attribute would delete last-N and invalidate those files. Option B, an explicit First/Last control over a positive count, was considered and not taken; the help text on the field explains the sign.                                       | [KXI-73189](https://kxl.atlassian.net/browse/KXI-73189) |
| [KXI-70499](https://kxl.atlassian.net/browse/KXI-70499) | Task  | Medium   | Datasource save button is confusing                                                  | Triage: the described confusion is gone. Refresh and Save are separate buttons, and the editor is `TextDocument`-backed so dirty state, undo and save are VS Code's — the RFC makes Save a deliberate delegation to `workbench.action.files.save`. "Can we remove it?" is now a preference call, not a bug.                                                                                                                                    | [KXI-73189](https://kxl.atlassian.net/browse/KXI-73189) |
| [KXI-70500](https://kxl.atlassian.net/browse/KXI-70500) | Story | Medium   | Datasource should have a clear button to reset the form in each tab                  | Triage: premise removed. There are no tabs and no permanently-present optional fields to reset — optional parameters do not exist until added via `+ Add parameter`, and each has its own trash button. Re-file if a "clear all rows" on a filled getData is still wanted.                                                                                                                                                                     | [KXI-73189](https://kxl.atlassian.net/browse/KXI-73189) |

## Unfiled — defects found while working this list (12)

None of these had a Jira issue. Each was found by exercising the code rather
than by reading a ticket, so each is recorded here with what made it reachable.

| Area        | Where                                                                 | What                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | State                                                                                                                                                   |
| ----------- | --------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| UDA         | `createUDAReturn`, `src/utils/uda.ts`                                 | Threw on a UDA registered without a return: `uda?.return.type` guards `uda`, not `return`, and `metaReturn` is optional in q. One such UDA in the meta took down `parseUDAList` and with it `parseQueryList`, leaving the editor with **no** APIs at all, builtins included.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Fixed — worst of the set, for the blast radius                                                                                                          |
| UDA         | `isInvalidRequiredParam`, `processUDAParams`                          | `false` and `0` counted as "not answered". A required boolean could never be run as `false`, and an unchecked optional boolean was sent as `""`. The checkbox sets a real boolean (`renderCheckbox` → `setParam(param, checked)`), so this was reachable from the form, not only a hand edit.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Fixed — both now test `undefined`/`null`/`""` only                                                                                                      |
| UDA         | `kdbQueryView.message`                                                | A stored query was never given the distinguished parameters, unlike one picked from the dropdown. A query whose `query` had no `params` crashed every render; one lacking the distinguished set offered no table/labels/scope/startTS. Reachable through conversion — `toQuery` (queryCommand.ts:87) returns a converted datasource's `uda` verbatim.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Fixed — stored queries now get the same treatment                                                                                                       |
| getData     | `serializeRows`, `src/models/query.ts`                                | A single `in` or `within` value was sent as a scalar, though "when using `in` or `within` this should be a list" ([getData](https://code.kx.com/insights/1.19/api/database/query/get-data.html)). `sym in AAPL` went out as `["in","sym","AAPL"]`. Two or more values were always right, which is why it survived.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Fixed — `forcesList`, those two operators only                                                                                                          |
| Both        | `renderChoice`, `kdbQueryView.ts`                                     | An optional choice parameter deleted itself when its dropdown was opened: the `kdb-select` and the remove button shared one `<label>`, and a label forwards its click to its first labelable descendant — a custom element does not qualify. Reachable before this branch via the UDA distinguished `table`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Fixed — both choice renderers use a `div`, like `renderRows`                                                                                            |
| Results     | `formatResult`, `render` (notebookController.ts)                      | HTML injection on every text and table result: `formatResult` escaped nothing and the results view injects with `unsafeHTML`, so a q expression containing `<` was parsed as a tag — `{x<y}` swallowed the rest of the line, which took out the caret line of a stack trace. The notebook renderer built `<th>`/`<td>` and its "Execution stopped" message the same way. Found while working KXI-71824, whose whole point is a trace that lines up.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Fixed — `escapeHtml` at all four sites; the e2e test that pinned the markup rendering asserts text now                                                  |
| getData     | `toValues`, `parseTables`                                             | Filter values are coerced to numbers whenever they parse as one, because the row field cannot see the column's type: `sym = 600519` goes out as the number and cannot match. `meta.schema[].columns[].typ` carries the type and `parseTables` drops it. Carrying it through would also let KXI-70491's columns validate. **Cheaper than noted, tested live 2026-09-01 on PINK**: the gateway casts filter values to the column type itself, so string values match on int, short, long, date, timestamp, time and guid columns (`=`, `in`, `within`, `>` all verified) — the coercion buys nothing there and is not load-bearing. What it costs is symbol columns: a numeric-looking value is rejected outright with **HTTP 400** `Failed symbols conversion of 600519f`, a visible error rather than the silent no-match assumed above. Dropping the coercion looks sufficient; carrying `typ` through `parseTables` is only needed for the display-literal cases in the row below.                                                                                                                                                                                                                                             | **Open** — file before working; the coercion is load-bearing for numeric columns                                                                        |
| getData     | filter values, `serializeRows` → gateway                              | The literals the results grid shows do not filter. Found live 2026-09-01 on PINK's `insightsMultitypeTable`: `1b` on a boolean column returns the **`0b`** rows (6 of 10) while `1`/`true` correctly return the four `1b` rows — a silently wrong answer, and `1b` is exactly what the payload and a q user write; `0x7e` on a byte column and `2022.04m` on a month column match nothing, while `7e`/`126` and `2022.04` match one row each. Float `=` on a displayed value never matches, because the payload rounds (`7.111716` against more precision on the DAP).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Open** — no ticket. Needs the column type at the row (same fix as above) plus deciding whether the form accepts q literals or normalises them         |
| UDA         | `processUDAParams`, `src/utils/uda.ts`                                | Dictionary parameters go to the gateway as JSON **strings** and are rejected. `serializeRows` stores `labels`/`scope` as `JSON.stringify(...)`, `processUDAParams` copies `param.value` through untouched, and nothing parses it on the way out. Verified live 2026-09-01 on PINK (`POST servicegateway/insightsUda/tableAPI`): `"scope": {…}` answers **200**, `"scope": "{…}"` answers **400** `Argument 'scope' is not a dictionary`, and the same for `labels` (`rc: 5`, `Invalid labels. Not a dictionary, raw=…`). So the row grid KXI-70494 added never lands a value, and KXI-70496 cannot work until this is fixed. Fixed 2026-09-03: `processUDAParams` parses every JSON-typed parameter — `ext.jsonTypes`, so lists as well as dictionaries — through `parseValue`, and text that does not parse is reported as the error rather than left for the gateway to answer 400 to. A blank one stays the empty string it was, except `scope`, which the transport drops when no target was chosen. `scope` itself is now exempt from the parse: it holds a target string, resolved in `retrieveUDAtoCreateReqBody` where the connection is at hand. The scratchpad path sends `parameterTypes` and is still untested live. | Fixed — and KXI-70496 with it                                                                                                                           |
| Connections | `ScratchpadLogger.open`, `src/classes/scratchpadLogger.ts`            | Every websocket `error` was raised as a modal notification, including the `Unexpected server response: 500` the handshake gets for the first seconds after a **successful** connect, while the scratchpad is still being provisioned. The socket heals itself — `error` is followed by `close`, which schedules a backoff reconnect — so the popup announced a failure that was already over. Reported from IUAT 2026-09-01.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Fixed — the first three attempts log at `DEBUG`, a failure that outlives them alerts once, a successful `open` re-arms it                               |
| Connections | `ScratchpadLogger.disconnect`                                         | A pending reconnect survived disconnection. `disconnect()` closed the socket but left the backoff timer armed; when it fired, `open()` set `isManualClose` back to `false` and built a new socket for a connection that was gone — and `InsightsConnection.disconnect` had already dropped the `scratchpadLogger` reference, so nothing was left to close it again. Reachable through the row above: handshake fails, user disconnects inside the backoff window.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Fixed — `disconnect` clears the timer                                                                                                                   |
| Connections | `getServers`/`getInsightsServers`, `src/commands/workspaceCommand.ts` | The connection picker and the connections tree disagreed on order. `workspaceCommand` keeps its own copies of these helpers that map the settings object straight to aliases, so the picker listed connections roughly in the order they were added, while the tree's `utils/core` versions `localeCompare` on the alias.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Fixed — both helpers sort by alias; not delegated to `utils/core`, whose `getInsights()` writes the settings whenever there are no Insights connections |
